import {formatNumber} from '../../utils/util';

let arr = [1,2,3];

const splitArr = function() {
  arr = arr.map(a => a + 1);
  formatNumber(1);
  return arr.toString();
};

module.exports = {
  arr,
  splitArr,
};
